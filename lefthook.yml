pre-commit:
  parallel: true
  jobs:
    - name: lint-fix
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx biome check --write --files-ignore-unknown=true {staged_files} >/dev/null 2>&1 || true
      stage_fixed: true
