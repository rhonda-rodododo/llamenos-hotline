<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration for llamenos.

    Production: Only allows HTTPS connections (system default).
    Debug: Allows cleartext (HTTP) to localhost for development.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext to localhost for development only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
